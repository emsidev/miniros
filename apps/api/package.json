{
  "name": "@miniros/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsdown src/server.ts --format esm --no-fixed-extension --target es2022 --out-dir dist --clean",
    "start": "node dist/server.js",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "echo No api tests yet"
  },
  "dependencies": {
    "@hono/node-server": "^1.19.17",
    "@miniros/contracts": "workspace:*",
    "@miniros/domain": "workspace:*",
    "hono": "^4.13.7"
  }
}
