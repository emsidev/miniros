{
  "name": "@miniros/e2e",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "echo Add e2e tooling when the first shared flow is stable",
    "build": "node -e \"console.log('Offline workflow suite: run pnpm e2e')\"",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "4.1.11",
    "fake-indexeddb": "^6.2.4",
    "@electric-sql/pglite": "^0.3.14",
    "drizzle-orm": "^0.45.2",
    "@miniros/db": "workspace:*",
    "@miniros/contracts": "workspace:*",
    "@miniros/domain": "workspace:*"
  }
}
