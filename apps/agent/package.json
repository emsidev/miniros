{
  "name": "@miniros/agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsdown src/index.ts --format esm --no-fixed-extension --target es2022 --out-dir dist --clean",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "echo No agent tests yet"
  },
  "dependencies": {
    "@miniros/domain": "workspace:*",
    "@miniros/sdk": "workspace:*"
  }
}
