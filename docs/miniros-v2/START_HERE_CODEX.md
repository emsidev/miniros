# Start here — instruction to Codex

Implement the Miniros local-first booth redesign in the existing `emsidev/miniros` repository using the implementation pack supplied with this message. This is an execution request, not a request to produce another roadmap.

**Use actual subagents to complete the execution plans.** Act as the coordinating lead. Delegate bounded work to domain/data, mobile/local-connectivity, owner/backend, and independent QA/security agents. Use at most three concurrent subagents initially; roles can be reused. Parallelize only independent tasks with non-overlapping file ownership. One integrator owns shared contracts, migrations, dependency manifests, and lockfiles. Require an independent review before accepting a plan. Do not merely simulate an agent conversation. If the current environment cannot spawn subagents, report that limitation and complete safe work serially using the same checkpoints; do not claim agents ran.

Read the existing `AGENTS.md` instructions and inspect the actual checkout first. The reference baseline is `main` at `d073cbbc0f78bf2e9142a663a70009c60158c8a1`; do not reset or downgrade a newer checkout. Reconcile the pack with current code and preserve unrelated work. The attached plan supersedes earlier product proposals that made the owner select products or allocate opening inventory for every shift.

The owner configures the business catalog, recipes, inventory categories/units, and required departure checklist once. For each shift the owner enters only its date, opening/closing times, venue/address, and assigned cashier/prep staff. The application automatically snapshots the business configuration. Staff completes the departure checklist, enters actual opening stock, operates cashier/prep on two phones, and closes. Staff operations are local-first and survive internet outages; local phone-to-phone order delivery does not depend on Supabase. Cloud sync provides owner visibility when reachable.

Read `MASTER_PLAN.md`, `PRODUCT_AND_ARCHITECTURE.md`, `AGENT_EXECUTION_RULES.md`, and `TEST_STRATEGY.md`. Then execute `execution-plans/EP00-*.md` through `EP19-*.md` in dependency order. With the combined-file version, materialize its labeled sections into those paths under `docs/miniros-v2/` first. Keep root `AGENTS.md` concise and link to the pack; do not paste the entire pack there or replace existing instructions wholesale.

For each plan: inspect the relevant code; record the task and file ownership; implement a minimal slice; add and run its specified tests; obtain independent review; integrate; rerun affected checks; and update `EXECUTION_STATUS.md` plus an evidence report. Use the golden fixture as an independently calculated oracle, not production data. Continue to the next unblocked plan instead of asking for permission after each ordinary development step. Do not stop after scaffolding, a UI mockup, or a build.

Preserve the existing database and local journals. Do not reset Supabase, erase IndexedDB, put secrets in clients/QRs, weaken authorization, install a second sync engine without an ADR, or run production migrations/deployments. Use local/disposable databases and approved staging only. Inspect command targets: the baseline `db:migrate` script runs `supabase db push`, which is not an automatically safe local test.

A placeholder test command, web export, mocked transport, or screenshot is not proof of native reliability. Record each test as passed, failed, blocked, or not run. Real-device tests, signing, hosted credentials, and pilot approval remain explicit gates when unavailable. Continue independent implementation work, but do not mark those gates passed or release the app. Never invent logs or results. At session boundaries persist the exact next task and blocker; no promise of unattended future work.

Begin EP00 now. Your final execution report must list completed plans, changed files/commits, commands and results, outstanding gates, migration/rollback implications, and the next actionable step.
